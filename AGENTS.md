# AGENTS.md

This repository keeps a running handoff in [LOG.md](/Users/ranger/CLOUDMANAGER/LOG.md).

Rules for future Codex or agent runs:

1. Read `AGENTS.md` and `LOG.md` before making assumptions about current state or repeating discovery work.
2. Treat `LOG.md` as the canonical session handoff for recent changes, validations, blockers, and next steps.
3. After any substantial change, append or refresh `LOG.md` with:
   - date
   - user request handled
   - key code or UI changes
   - validation performed
   - remaining risks or follow-up work
4. Keep `LOG.md` concise but sufficient for the next run to resume without needing prior chat history.
5. If work is inside `cloudmanager_crew/`, also read [cloudmanager_crew/AGENTS.md](/Users/ranger/CLOUDMANAGER/cloudmanager_crew/AGENTS.md), which is specific to that subtree and does not replace this root handoff rule.

